rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발용: 모든 인증된 사용자가 모든 문서에 접근 가능
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
