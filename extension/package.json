{
  "name": "extension",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:extension": "vite build && rm -rf ~/Documents/chromeExtension && cp -r dist ~/Documents/chromeExtension",
    "preview": "vite preview",
    "release": "vite build && zip -r dist.zip dist/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^5.0.0",
    "firebase": "^11.0.2",
    "tailwindcss": "^3.4.16"
  }
}
